# Задание 4. Самостоятельная работа, модифицируем базу данных, выполняем миграцию данных, добавляем операции с данными.

## Самостоятельная работа

1) Добавьте кнопку удаления заметки на элемент списка заметок;
2) Добавьте кнопку очистки базы данных - удаления всех заметок;
3) Удалите поле "title" класса Note, пусть заметка сохраняет только текст;
4) Выполните миграцию на новую версию базы данных;
5) Добавьте кнопку сортировки элементов по алфавиту;

```markdown
Выполненные шаги:
1. Добавлена кнопка удаления заметки:
◦ В NoteDao использован существующий метод @Delete.
◦ В NoteViewModel добавлен метод deleteNote(note: Note).
◦ В NoteApp.kt в элемент ListItem добавлен trailingContent с иконкой удаления.
2. Добавлена кнопка очистки базы данных:
◦ В NoteDao добавлен метод @Query("DELETE FROM notes").
◦ В NoteViewModel добавлен метод clearAllNotes().
◦ В NoteApp.kt в Scaffold добавлен topBar (использован CenterAlignedTopAppBar) с кнопкой «Удалить всё».
3. Удалено поле "title" класса Note:
◦ В классе Note закомментировано поле title.
◦ Обновлен метод addNote в NoteViewModel, теперь он принимает только text и emoji.
◦ В UI (AddNoteScreen) удалено текстовое поле для заголовка.
◦ В списке заметок основной текст теперь отображается в заголовке элемента (headlineContent).
4. Выполнена миграция базы данных (v2 -> v3):
◦ Версия базы данных в AppDatabase увеличена до 3.
◦ Реализован объект MIGRATION_2_3, который создает новую таблицу без поля title, переносит данные и переименовывает таблицу (безопасный способ удаления колонки в SQLite).
◦ Миграция добавлена в Room.databaseBuilder.
5. Добавлена кнопка сортировки по алфавиту:
◦ В NoteDao добавлен метод getAllNotesSortedByText().
◦ В NoteViewModel добавлено состояние isSortedAlphabetically и метод toggleSort(). Список заметок теперь переключается между обычной сортировкой по дате и алфавитной.
◦ В topBar добавлена кнопка для переключения сортировки.
История коммитов:
1. Step 1: Добавьте кнопку удаления заметки на элемент списка заметок
2. Step 2: Добавьте кнопку очистки базы данных - удаления всех заметок
3. Step 3: Удалите поле 'title' класса Note, пусть заметка сохраняет только текст
4. Step 4: Выполните миграцию на новую версию базы данных
5. Step 5: Добавьте кнопку сортировки элементов по алфавиту
```